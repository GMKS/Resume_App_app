import 'dart:async';

Future<String> getResumeTips(String resumeContent) async {
  // Simulate an AI call (replace with real API if needed)
  await Future.delayed(const Duration(seconds: 1));
  return "Tip: Use action verbs, quantify achievements, and keep your resume concise!";
}